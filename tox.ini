[tox]
envlist = py39
skipsdist = True

[testenv]
deps =
    pytest
    pytest-cov
commands = pytest --cov=mandelbrot_julia.mandelbrot_julia --cov-report=xml --cov-config=tox.ini --cov-branch

[coverage:run]
source = mandelbrot_julia/mandelbrot_julia
